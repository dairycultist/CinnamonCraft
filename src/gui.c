typedef struct {

	Mesh *mesh; // remeshed when window is resized
	float height; // float between 0..1 representing proportion of window height

} GuiMesh;

// TextMesh?

// returns width of GuiMesh in pixels given the current window size
int get_practical_width(GuiMesh *gui) {

}

// returns height of GuiMesh in pixels given the current window size
int get_practical_height(GuiMesh *gui) {
	
}

void draw_centered() {

}